# Previously ran `clerk morning` daily. Daily clerk is **disabled** in this fork —
# use `clerk weekly` (see `clerk-weekly.yml` and `scripts/cron-clerk-weekly.sh`) instead.
#
# This workflow is kept as a no-op so existing repo Actions that still reference it
# do not fail unexpectedly.

name: Clerk morning (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::notice::Daily clerk morning is disabled. Use \`clerk weekly\` (workflow clerk-weekly.yml or local cron)."
